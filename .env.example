# Star-Fire 服务端环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置值

# ==================== 服务器配置 ====================
# 服务器端口，默认 :8080
SERVER_PORT=:8080

# ==================== 连接配置 ====================
# 保持连接时间（秒），默认 30 秒
KEEPALIVE_TIME=30

# 最大延迟时间（秒），默认 5 秒
MAX_LATENCY=5

# 聊天最大时间（秒），默认 300 秒（5分钟）
CHAT_MAX_TIME=300

# WebSocket 缓冲区大小（字节），默认 1048576（1MB）
WS_BUFFER=1048576

# ==================== JWT 配置 ====================
# JWT 密钥，建议使用复杂的随机字符串
JWT_SECRET=your-very-secure-jwt-secret-key-here-change-this

# JWT 过期时间（小时），默认 24 小时
JWT_EXPIRY=24

# ==================== API Key 配置 ====================
# 每个用户最大 API Key 数量，默认 3 个
MAX_API_KEYS_PER_USER=3

# 默认 API Key 过期天数，默认 30 天
DEFAULT_KEY_EXPIRY=30

# ==================== 负载均衡配置 ====================
# 负载均衡算法，可选值：round-robin, least-connections, random
LBA=round-robin

# ==================== 邮件配置 ====================
# 邮件服务器主机名（如需要邮件功能请配置）
EMAIL_HOST=smtp.gmail.com

# 邮件服务器端口，默认 587
EMAIL_PORT=587

# 邮件用户名
EMAIL_USER=your-email@example.com

# 邮件密码或应用程序密码
EMAIL_PASSWORD=your-email-password

# 发件人邮箱地址
EMAIL_FROM=noreply@yourcompany.com

# ==================== 数据库配置 ====================
# 注意：如果项目还有数据库相关配置，可以在此添加
# 例如：
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=starfire
# DB_USER=postgres
# DB_PASSWORD=password

# ==================== 其他配置 ====================
# 可根据项目需要添加其他环境变量
# 例如：
# LOG_LEVEL=info
# DEBUG=false
# CORS_ORIGIN=*
